# In R, variables are dynamically typed (ie. you do not have to specify the type of data it'll hold)

x = 10 #here we assign the number 10 to the variable x
print(x)
x = "10" #here we assign a string to the variable x
print(x)