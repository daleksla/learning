# R has many libraries that are useful esp. regarding data visualisation
# To install a library, use the 'install.packages("<package-name>")' function
# To then make use of the library, use the 'library(<package-name)' function

install.packages("tidyverse")
library(tidyverse)

#You are now free to use the functions the library has to offer